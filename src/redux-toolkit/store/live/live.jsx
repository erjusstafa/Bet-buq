import { createSlice } from "@reduxjs/toolkit";

const liveSlice = createSlice({
  name: "live",
  initialState: {},
  reducers: {},
});
export const {} = liveSlice.actions;

export default liveSlice.reducer;
