/* import React from 'react'
import { useSelector } from 'react-redux';

const SliderCasino = () => {

    let slideCasino = useSelector(
        (state) => state.betbuqsport.myApi?.result?.casino_live?.center
    );
    console.log("casino", slideCasino);
    return (
        <div className="slider-casino">
           {
               slideCasino.map(S => (

               ))
           }
        </div>
    )
}

export default SliderCasino
 */